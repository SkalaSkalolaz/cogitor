# AI Cogitor

[![License](https://img.shields.io/badge/License-BSD_3--Clause-blue.svg)](https://opensource.org/licenses/BSD-3-Clause)  
[![Go Version](https://img.shields.io/badge/go-1.21+-blue.svg)](https://golang.org/dl/)

Version: **1.0.1**

## Описание проекта

AI Cogitor — интеллектуальный ассистент для разработчиков с интеграцией LLM (Large Language Models). Поддерживает CLI, веб-интерфейс и десктопный режим. Генерирует, анализирует и исправляет код на множестве языков программирования с автоматическим поиском в интернете и RAG-режимом для работы с локальными данными.

## Ключевые возможности

- **Мультипровайдерная архитектура**: Ollama, OpenRouter, Pollinations, Phind, произвольные URL API
- **Генерация и исправление кода**: автоматическая компиляция, запуск и исправление ошибок через LLM
- **DIFF-режим**: частичное редактирование файлов без полной перезаписи
- **RAG-режим**: загрузка локальных данных (txt, json, csv, md, xml, yaml) для контекстных ответов
- **Веб-поиск**: автоматический поиск актуальной информации через DuckDuckGo
- **Три режима работы**: CLI, веб-сервер с WebSocket, GUI (webview)
- **Управление контекстом**: сохранение/загрузка сессий, суммаризация, экспорт
- **Интеграция с буфером обмена**: чтение и автоматическое копирование ответов

## Поддерживаемые языки

Go, Python, C, C++, Fortran, Ruby, Kotlin, Swift, Lisp, Assembly, HTML

## Установка

```bash
git clone https://github.com/SkalaSkalolaz/cogitor.git
cd cogitor
go build -o cogitor .
```

### Зависимости

```bash
go mod tidy
```

Для GUI-режима требуется установка webview:
- **Linux**: `sudo apt-get install libwebkit2gtk-4.0-dev`
- **macOS**: встроенная поддержка
- **Windows**: требуется WebView2

## Быстрый старт

```bash
# CLI режим с локальным Ollama
./cogitor ollama gemma3:4b

# Веб-сервер на порту 8080
./cogitor --server

# GUI приложение
./cogitor --gui

# С OpenRouter и API ключом
./cogitor openrouter deepseek/deepseek-chat-v3.1:free sk-or-...
```

## Использование

### Командная строка

```bash
./cogitor [ПРОВАЙДЕР] [МОДЕЛЬ] [API-ключ]
./cogitor [ОПЦИИ]
```

### Опции запуска

| Опция | Описание |
|-------|----------|
| `--gui` | Запуск GUI приложения |
| `--server [PORT]` | Запуск веб-сервера (порт по умолчанию: 8080) |
| `-i, --input FILE` | Файл с описанием задачи |
| `-p, --provider NAME` | Провайдер LLM |
| `-m, --model NAME` | Модель LLM |
| `-k, --key KEY` | API ключ |
| `--no-search` | Отключить веб-поиск |
| `-h, --help` | Справка |
| `-v, --version` | Версия |

### Специальные маркеры в запросах

| Маркер | Действие |
|--------|----------|
| `@filename` | Прикрепить файл к запросу |
| `@all` | Прикрепить все файлы проекта |
| `@http://...` | Загрузить содержимое веб-страницы |
| `$cod` | Режим генерации кода |
| `$diff` | Режим частичного редактирования (DIFF) |
| `$int` | Открыть URL в браузере |

### Служебные команды

**Контекст:**
```
:clean              — Очистить историю
:pop [n]            — Удалить последние n обменов
:ctx                — Статистика контекста
:limit <число>      — Установить лимит контекста
:summarize          — Сжать контекст до сводки
```

**RAG-режим:**
```
:data <путь>        — Загрузить файлы данных
:data status        — Статус RAG
:data               — Отключить RAG
```

**Сессии:**
```
:save [имя]         — Сохранить сессию
:load <имя>         — Загрузить сессию
:ls                 — Список сессий
:rm <имя>           — Удалить сессию
:export [fmt]       — Экспорт (md/txt/json)
```

**I/O:**
```
:sh <команда>       — Выполнить shell-команду
:cd <path>          — Сменить директорию
:pwd                — Текущая директория
:dir [-alh]         — Содержимое директории
:open <file>        — Открыть в редакторе
:clip               — Показать буфер обмена
:clip+              — Добавить буфер в запрос
:copy [on|off]      — Авто-копирование ответов
```

**Провайдеры:**
```
:providers          — Список провайдеров
:provider <name>    — Сменить провайдера
:models             — Список моделей
:model <name>       — Сменить модель
```

**Настройки:**
```
:set <key> <value>  — Изменить настройку
:get [key]          — Показать настройки
:debug [on|off]     — Режим отладки
:skip [on|off]      — Пропуск установки зависимостей
:reset              — Сбросить настройки
```

## Примеры

### Генерация кода

```
👤 Вы: $cod Напиши HTTP сервер на Go с эндпоинтом /health
```

### Частичное редактирование (DIFF)

```
👤 Вы: $diff @main.go Добавь логирование в функцию handleRequest
```

### Работа с веб-страницами

```
👤 Вы: @https://go.dev/doc/effective_go Суммаризируй основные принципы
```

### RAG-режим

```
👤 Вы: :data ./docs/
✅ RAG-режим активирован (5 документов)

👤 Вы: Найди информацию о конфигурации базы данных
```

### Поиск в интернете

```
👤 Вы: Найди в интернете последние изменения в Go 1.22
```

## Архитектура

```
cogitor/
├── main.go              # Точка входа, парсинг аргументов
├── assistant.go         # Основная логика ассистента
├── llm.go               # Интеграция с LLM провайдерами
├── server.go            # Веб-сервер и WebSocket
├── commands.go          # Обработка служебных команд
├── context.go           # Управление контекстом диалога
├── codeparser.go        # Парсинг кода из ответов LLM
├── coderunner.go        # Компиляция и запуск кода
├── diff.go              # DIFF-патчи с fuzzy-валидацией
├── fileparser.go        # Парсинг файловых ссылок
├── websearch.go         # Поиск через DuckDuckGo
├── project_analyzer.go  # Анализ структуры проекта
├── installer.go         # Установка зависимостей
├── config.go            # Конфигурация
├── stats.go             # Статистика использования
├── terminal.go          # Интерактивный ввод с историей
├── clipboard.go         # Работа с буфером обмена
├── markdown.go          # Рендеринг Markdown
├── utils.go             # Вспомогательные функции
├── webview.go           # GUI режим
└── web/
    └── index.html       # Веб-интерфейс
```

## Конфигурация

Конфигурация хранится в `~/.cogitor/config.json`:

```json
{
  "context_limit": 10,
  "max_retries": 10,
  "web_search": true,
  "debug_mode": false,
  "auto_execute": false,
  "skip_install": false
}
```

Сессии сохраняются в `~/.cogitor/sessions/`.

## Веб-интерфейс

При запуске с `--server` доступен веб-интерфейс:

- **URL**: `http://localhost:8080`
- **WebSocket**: `ws://localhost:8080/api/ws`
- **API endpoints**: `/api/status`, `/api/sessions/*`, `/api/rag/*`, `/api/provider/*`

Интерфейс поддерживает:
- Чат с подсветкой синтаксиса и Markdown
- Управление контекстом и сессиями
- RAG-режим с загрузкой файлов
- Смену провайдера и модели на лету

## Требования

- Go 1.21+
- Для локальных моделей: [Ollama](https://ollama.ai/)
- Для буфера обмена (Linux): `xclip` или `xsel`
- Для GUI: webkit2gtk (Linux)

## Лицензия

License-BSD_3--Clause

---

**AI Cogitor** — ваш интеллектуальный помощник в разработке.
